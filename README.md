# DadOS v11

Clean consolidation build.

- Plan now calls `/api/plan` and changes based on the actual prompt.
- Go Somewhere calls `/api/activities` and returns real nearby places with links.
- Light warm stone / cream + olive palette, not black.
- Permanent saves retained.
- Gemini fallback logic retained.
- Visible `DadOS v11` marker confirms deployment.
